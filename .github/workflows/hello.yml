name: Hello world!

on:
        pull_request:
                types: [opened, reopened]
                branches:
                        - master

jobs:
        hello_world_job:
                runs-on: ubuntu-20.04
                steps:
                        - name: Say hello
                        run: |
                         echo "Hello World!"       
